# 0004. This Settings pass is IA plus visible Measurements

This pass ships only (1) an in-panel Settings form with sections, its own scroll, Done, and immediate apply, and (2) toggles for which Measurements appear in the Glance detail grid. Defaults stay all-on. Menu-bar label formats, city reorder/rename, extra refresh values, language, and alerts are out of scope.
