# 0007. Persist hidden Measurement IDs

Visibility is stored as the set of hidden Measurement IDs. An empty set means all Measurements are on. A Measurement added in a later version appears unless the user has hidden that ID. We do not persist the visible set.
